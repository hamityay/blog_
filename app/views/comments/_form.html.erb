
<%= form_for([@post, @post.comments.build]) do |f| %>
  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name, placeholder: 'name' %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body, placeholder: 'body', rows: 5 %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
